<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
     PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
     "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
     
<mapper namespace="com.example.project_team.mappers.savingsAdminMapper">
	<!-- SavingsAdminList -->
	<select id="SavingsAdminList" resultType="com.example.project_team.dto.SavingsDTO">
		SELECT * FROM TEAM_SAVINGS_PRODUCT ORDER BY jNo
	</select>
	
	<!-- insertSavings -->
	<insert id="insertSavings" parameterType="com.example.project_team.dto.SavingsDTO">
		INSERT INTO TEAM_SAVINGS_PRODUCT
			VALUES((SELECT NVL(MAX(yNo)+1,1) FROM TEAM_SAVINGS_PRODUCT), #{jName}, sysdate
					, #{interestRate}, #{jSummary},#{jType}, #{jMinPrice}, #{jMaxPrice}, #{jMinDate}, #{jMaxDate}					)
	</insert>
	
	<!-- findByNum / 1건 조회 -->
	<select id="findByNum" parameterType="int" resultType="com.example.project_team.dto.SavingsDTO">
		SELECT * FROM TEAM_SAVINGS_PRODUCT WHERE jNo = #{jNo}
	</select>
	
	<!-- updateSavings -->
	<update id="updateSavings" parameterType="com.example.project_team.dto.SavingsDTO">
		UPDATE TEAM_SAVINGS_PRODUCT
			SET jName=#{jName}, interestRate=#{interestRate}, jSummary=#{jSummary}
				,jType=#{jType}, jMinPrice=#{jMinPrice}, jMaxPrice =#{jMaxPrice}, jMinDate=#{jMinDate}, jMaxDate=#{jMaxDate}
			WHERE jNo=#{jNo}
	</update>
	
	<!-- deleteSavings -->
	<delete id="deleteSavings" parameterType="int">
		DELETE FROM TEAM_SAVINGS_PRODUCT WHERE jNo=#{jNo}
	</delete>
</mapper>