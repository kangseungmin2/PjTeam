<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	  
<mapper namespace="com.example.project_team.mappers.UtilityMapper">

<select id="utilityList" parameterType="String" resultType="com.example.project_team.dto.UtilityDTO">
	SELECT utilityType,accountNum,utilityId,notificationDate,transactionDate,amount FROM team_utilities WHERE id=#{id} ORDER BY utilityType
</select>

</mapper>